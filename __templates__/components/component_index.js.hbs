import { I{{ pascalCase name }} } from "@/components/{{ pascalCase name }}/data";
import { {{ pascalCase name }} as Layout } from "./Layout";

export const {{ pascalCase name }} = (props: I{{ pascalCase name }} ) => {
  const layoutProps = {
    ...props,
  };

  return <Layout {...layoutProps} />;
};