impor S from './styles'
import { I{{ pascalCase name }}Layout } from '@/components/Basics/{{ pascalCase name }}/data'

export const {{pascalCase name}} = ({ data }: I{{pascalCase name}}Layout ) => (
  <S.Container flex={1} id={`{{pascalCase name}}`}>
    <S.Text>{`{{pascalCase name}}`}</S.Text>
  </S.Container>
)