import { I{{ pascalCase name }} } from '@/containers/{{ pascalCase name }}/data';
import { {{ pascalCase name }} as Layout } from './Layout';

export default function {{ pascalCase name }} (props: I{{ pascalCase name }}) {
  const layoutProps = {
    ...props,
  }
  
  return <Layout {...layoutProps} />;
}