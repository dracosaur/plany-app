import { ILoginPageLayout } from '@/containers/LoginPage/data'
import S from './styles';

export const LoginPage = ({ data }: ILoginPageLayout) => (
  <S.Container id={`LoginPage`}>
    <S.Text>{`LoginPage`}</S.Text>
  </S.Container>
)
